<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    >

    <TextView
        android:id="@+id/lblListRowIBAN"
        android:layout_width="fill_parent"
        android:layout_height="70dp"
        android:textSize="24sp"
        android:textStyle="bold"
        android:paddingStart="10dp"
        android:paddingEnd="10dp"
        android:textColor="@color/colorGrayDetail"
        android:layout_toStartOf="@+id/lblListRowCurrency"
        android:gravity="center|left"
        />


    <TextView
        android:id="@+id/lblListRowCurrency"
        android:layout_width="wrap_content"
        android:layout_height="70dp"
        android:textSize="20sp"
        android:textStyle="bold"
        android:paddingStart="10dp"
        android:paddingEnd="10dp"
        android:textColor="@color/colorPrimary"
        android:layout_alignParentEnd="true"
        android:layout_alignTop="@+id/lblListRowIBAN"
        android:layout_alignBottom="@+id/lblListRowIBAN"
        android:gravity="center"
        />




</RelativeLayout>